# Avoid npm version check that can cause issues
update-notifier=false

# Increase network timeout for slow connections
network-timeout=100000

# Use legacy peer deps to avoid compatibility issues
legacy-peer-deps=true

# Force resolution of dependencies
force=true

# Disable strict SSL for problematic networks
strict-ssl=false
